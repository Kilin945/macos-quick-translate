#!/usr/bin/env bash
# Double-click this file in Finder to install QuickTranslate.
# (First time, if macOS blocks it: right-click → Open.)
cd "$(dirname "${BASH_SOURCE[0]}")"
./scripts/install.sh
echo
echo "All set. You can close this window."
